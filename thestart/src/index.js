// import React from "react";
// In React 17 and later, you don't need to import the React object to use JSX due to the new JSX Transform. So, in this current code, the line import React from "react"; is not necessary.
import ReactDOM from "react-dom/client";
import App from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
